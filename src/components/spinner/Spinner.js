import React from 'react'
import spinner from '../../img/loading-1.gif'
import './Spinner.css'

const Spinner = () => {
    return (
        <img className="imga" src={spinner} alt="Loading"/>
    )
}

export default Spinner
